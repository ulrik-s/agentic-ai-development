{
  "name": "sensor-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal sensor dashboard skeleton — agents fill in the rest",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "seed": "node data/seed.js",
    "test": "echo \"no tests yet — see issue #5\" && exit 0"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
