{
  "name": "sensor-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal sensor dashboard skeleton — agents fill in the rest",
  "main": "server.js",
  "scripts": {
    "start": "nodemon server.js",
    "start:once": "node server.js",
    "seed": "node data/seed.js",
    "reset": "bash ../reset.sh",
    "test": "playwright test"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "nodemon": "^3.1.0"
  }
}
